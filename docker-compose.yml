services:
  pipeline:
    build: .
    env_file: .env
    volumes:
      - ./service_account.json:/app/service_account.json:ro
      - ./.tokens.json:/app/.tokens.json
      - ./data:/app/data
      - ./logs:/app/logs
    restart: "no"
